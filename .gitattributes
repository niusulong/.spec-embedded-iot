# 统一文本行尾为 LF（入库时），避免混合 CRLF/LF 造成的大 diff。
# Windows 检出时的 CRLF 由各开发者的 core.autocrlf 设置控制，不影响。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.zip binary
*.bin binary
*.elf binary
*.axf binary
*.pcap binary
*.pdf binary

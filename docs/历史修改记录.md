# 历史修改记录（spec-knowledge-archiver）

> 按时间倒序。早期（2026-06-15 前）`skills/` 以 git submodule 形式维护，部分细节来自全仓提交记录。
> 本记录聚焦 `spec-knowledge-archiver` 与配套基础设施；插件整体演进（新技能/重命名/发版）一并收入。

## 2026-07-16（v1.10.0）

- **release：插件版本 1.9.0 → 1.10.0**（6 处配置同步：claude/codex/cursor plugin + marketplace + package + gemini-extension）。
- **fix(spec-qcx216-dump-analyzer)：入库前补 spec- 前缀**——原 `skills/qcx216-dump-analyzer` 因缺 `spec-` 前缀被 `.gitignore`（仅追踪 `skills/spec-*/`）忽略，从未入库。本次重命名为 `spec-qcx216-dump-analyzer` 使其可被追踪。SKILL.md `name` 字段同步；脚本文件名保留 `qcx216_` 前缀（与同级技能惯例一致，如 `uis8850_analyze.py`）。README/CLAUDE.md 技能表已先行登记该技能。

## 2026-07-15（v1.9.0）

- **release：插件版本 1.8.0 → 1.9.0**（6 处配置同步）。
- **feat(spec-uis8850-dump-analyzer) v1.0：新增 UIS8850 / N706-STD-B41F (Unisoc ARM Cortex-R + FreeRTOS) dump 分析技能**
  - 从 DTools 抓取的 ramdump 重建死机现场：`gBlueScreenRegs` ARM 寄存器、`osiPanic(udf #255)` 蓝屏机制、`gBlueScreenAbortType`、FreeRTOS 任务栈溢出检测（`0xa5a5a5a5` magic）、CP 核 assert 经 IPC 上报、ARM Thumb 栈回溯、堆状态、`pxCurrentTCB`/TCB 解析、ARM `addr2line`/`objdump` 源码映射。
  - 10 个分析脚本（`uis8850_analyze` / `unwind` / `threads` / `heap_state` / `cp_assert` / `wdt_reset` / `trace_decode` / `map_lookup` / `run_all` / `common`）。
  - 7 个参考文档（arm-bluescreen / arm-stack-unwind / cp-assert / freertos-stack-overflow / heap-and-trace / uis8850-memory-map / bug-report-template）。
  - `run_all.py` 一键归档到 `.spec/bug/{id}_{desc}/analysis/`（INDEX.md + _meta.json）。
- **refactor(spec-bug-analyzer)**：「分析先于修复」原则去掉 `IsMiniSystem` 平台特有细节，措辞更通用。

## 2026-07-15（spec-uis8852-dump-analyzer 增强，未单独发版）

- **feat(spec-uis8852-dump-analyzer)：新增 DWARF CFI 确定性回溯 + 断言模式推理**（来源：Claude 会话 24531c49 的改动，原仅存于 `.claude` 插件缓存，未入仓库）。
  - 新增 `scripts/cfi.py`（DWARF `.debug_frame` CFI 引擎：FDE 索引 / 全寄存器规则表）。
  - 新增 `scripts/unwind_cfi.py`（CFI 确定性回溯 CLI：崩溃链 + 全任务逐线程链 + 线程归属）。
  - 新增 `scripts/assert_reason.py`（断言模式推理：scheduler 栈检查 → 自动判定溢出线程 + 根因链）。
  - 新增 `references/cfi-unwind-guide.md`。
  - 修改 `threads.py`（抽出 `enumerate_tcbs`/`find_thread_by_stack` 供三脚本共享）、`run_all.py`（集成 09_unwind_cfi / 10_assert_reason，共 10 个脚本）、`uis8852_analyze.py`（输出指引优先看 09/10）、SKILL.md（首选 CFI、断言判定陷阱、脚本表）。

## 2026-07-14（v1.8.0）

- **release：插件版本 1.7.0 → 1.8.0**（6 处配置同步）。
- **feat(skills)：脚本输出统一归档为文档**（与 uis8852 标杆一致），便于核对和留痕。
  - **spec-bug-analyzer（v3.6→v3.7）**：`pcap_analyzer.py` 5 个子命令（flows/show/around/search/decode）加 `--report` 参数，tee 模式同时输出 stdout + markdown 文件（含运行时间/pcap 路径/子命令/完整输出）；`log_analyzer.py` 的 stats/compare 子命令补 `--report`（search 已有），统一归档能力；指南文档同步 `--report` 用法和推荐路径。
  - **spec-ec626-dump-analyzer（v1.1）**：新增 `run_all.py`，编排 `full-analyze` + `scan-memp -v` + `scan-heap -v`，生成 `analysis/{01,02,03}_*.txt` + `INDEX.md`（自动提取结论）+ `_meta.json`（追溯上下文）。
  - **spec-asr1603-dump-analyzer（v1.1）**：新增 `run_all.py`，编排 `full-analyze → ddr_code_compare → stack_analysis → axf_disasm`，从 `full-analyze` 输出正则提取 PC/LR/DDR_BASE 跨脚本传递，生成 `analysis/{01-04}_*.txt` + `INDEX.md` + `_meta.json`。
- **test**：`test_pcap_analyzer.py` 补 `--report` 端到端测试 + scapy 段错误重试机制（66 项测试全过）。

## 2026-07-13（v1.7.0）

- **release：插件版本 1.6.0 → 1.7.0**（6 处配置同步）。
- **feat(spec-uis8852-dump-analyzer, examples)：新增 UIS8852/N706C RISC-V dump 分析技能 + CTM2M 测试用例示例**
  - `skills/spec-uis8852-dump-analyzer`：UIS8852/N706C（Unisoc RISC-V RV32 + RT-Thread）平台 crash dump 分析技能，含 dlmalloc 堆遍历、帧感知栈回溯、IRQ 定位、RISC-V `addr2line` 源码映射等脚本与参考文档。
  - `examples/`：新增 CTM2M AT 指令端到端测试用例（35 个 YAML）+ `env.yaml` + `suite-CTM2M.yaml`。
  - `CTWING_AEP_AT指令集.md`：CTWing AEP 平台 AT 指令集参考文档（N706 手册第 14 章）。
- **refactor：dump analyzer 命名对齐芯片型号**（与 ASR1603 风格统一，便于多 ASR/EC 芯片扩展）
  - `spec-asr-dump-analyzer` → `spec-asr1603-dump-analyzer`（`b1682cd`，git mv 13 文件保留历史）。
  - `spec-ec-dump-analyzer` → `spec-ec626-dump-analyzer`（`47a607a`，git mv 14 文件保留历史）。
  - 同步更新 8 个文件中的技能名引用：各 SKILL.md frontmatter / scripts、`spec-bug-analyzer`（SKILL + analysis-patterns）、`spec-init`、`spec-memory-leak-analyzer`、`spec-uis8852-dump-analyzer`、`spec-using-agents`、CLAUDE.md、README.md。
- **feat(spec-bug-analyzer v3.3→v3.6)：知识库检索改穷尽后询问 + 核心原则强化 + pcap 报文解析替代 Wireshark**
  - **v3.3→v3.4 知识库检索机制重构**：新增独立 Step 6「知识库检索（穷尽后询问）」，原 Step 2.5 置信度判据 + 自动检索改为客观穷尽清单（4 项全满足）+ 询问用户（一次询问，用户决定），禁止未经询问自动检索；后续步骤顺延。
  - **v3.4→v3.5 核心原则强化**（基于会话历史 21 条发现提炼）：新增「分析先于修复」（报告完成前禁止 Edit/Write 代码改动）；细化「证据优先」（关键节点强制标注 `[事实:行号]/[推断]/[待验证]`）；平台字段禁止带架构后缀（致 KB 向量分裂的实战纠错）；Step 2 补重启锚点切片、Step 4 补反驳证据自检闸、平台识别 `ls` 强制校验；`analysis-patterns` 新增通用分析手法（gap analysis / 多异常点对比 / 抓包验证 / 枚举回归 / heap 三数字）。
  - **v3.5→v3.6 pcap 报文解析**（替代 Wireshark）：新增 `scripts/pcap_analyzer.py`（5 子命令 flows/show/around/search/decode，scapy 全协议栈解码，覆盖 TCP/TLS 握手/HTTP/FTP/DNS/MQTT 3.1.1&5.0/CoAP）；新增 `references/pcap-analyzer-guide.md`（子命令速查 + 7 个典型工作流 + 判据避坑 + 能力边界）；新增 `scripts/test_pcap_analyzer.py`（65 个端到端测试，含 MQTT/CoAP，scapy 构造 pcap 自包含）；SKILL.md Step 2 补报文处理段，核心避坑（重传误判粘包）固化进指南。

## 2026-07-01（v1.6.0）

- **release：插件版本 1.5.0 → 1.6.0**（6 个配置文件同步）。
- **feat(spec-bug-analyzer v3.2→v3.3)：平台识别改用 git 仓库名 + 日志分析接入代码总结字段**
  - 平台识别从「日志/路径关键字猜测」改为「以 git 仓库名为权威源」，映射知识库 `platform/` 目录（EC626/ASR1603/N58/UIS8850/UIS8852），多义时询问用户不假设。
  - 检索平台名 vs dump 分派架构大类（EC 系/ASR 系）解耦。
  - Section 0 摘要新增「当前分支」字段（`git rev-parse --abbrev-ref HEAD`）。
  - Step 2 前置：加载相关模块 `code-summary` §8 字段字典，错误码现查含义免读源码。
- **feat(spec-code-summary v1.4→v1.5)：§8 新增「§8.0 快速检索清单」**——默认可见高价值关键字按分组列出，作为 bug-analyzer 的关键字来源，用法（大文件走 `log_analyzer.py -k` / 小文件 `grep`）由消费方按场景自决，不在产出方写死。

## 2026-06-25（docs）

- `cd02167` docs: 仓库名 `spec_v2` → `.spec-embedded-iot` 同步（README 安装命令 + marketplace source URL）。

## 2026-06-24（v1.5.0）

- **release：插件版本 1.4.0 → 1.5.0**。本次统一对齐各清单版本号：`.claude-plugin/plugin.json` + `marketplace.json`、`.codex-plugin/plugin.json`（1.2.0 落后）、`.cursor-plugin/plugin.json`（1.2.0 落后）、`package.json`（1.0.0）、`gemini-extension.json`（1.0.0）均升至 1.5.0。
- **refactor(knowledge)：知识库根路径迁移 `~/.agents/knowledge` → `~/.spec-embedded-iot/knowledge`**
  - `common.py` 的 `KNOWLEDGE_ROOT` 硬编码更新（脚本实际读写路径）；vector_db 数据随之迁移。
  - CLAUDE/AGENTS/docs/各技能 SKILL.md 的路径文字同步更新。检索/归档链路实测通过。
- **build(knowledge)：knowledge 剥离为独立 git 仓库，不再在主仓库追踪**
  - `knowledge/` 拥有独立 `.git`（remote: github.com/niusulong/knowledge.git）；主仓库通过 `.gitignore` 排除整个 `knowledge/`。磁盘文件原样保留，主仓库历史保留（未重写）。
- **feat(spec-init v2.0 → v2.1)：初始化时自动确保知识库就位**
  - 新增 Step 2：`~/.spec-embedded-iot/knowledge/.git` 不存在则 `git clone` 拉取；已存在则跳过（幂等）；路径存在但非 git 仓库则告警跳过不覆盖；克隆失败不阻塞 `.spec/` 创建。
  - 不自动 `git pull` 已有仓库（避免擅改用户数据）。vector_db 为生成产物，克隆后需手动 `embed_indexer.py build` 构建。

## 2026-06-18（v1.4.0）

- **release：插件版本 1.3.1 → 1.4.0**（planner 改为委托 writing-plans + 实战微调）。plugin.json 与 marketplace.json 同步。
- **refactor(spec-implementation-planner) v1.3 → v2.0：改为委托 superpowers:writing-plans 的纯代码级适配器**
  - 复用 writing-plans 成熟方法论（file-structure-first、任务拆解、内嵌代码步骤、interfaces、no-placeholders、self-review），不自行重造。
  - **两点硬约束**：① 所有代码符合 spec-neoway-coding-standards（Global Constraint）；② 输出到 `.spec/requirement/{项目ID}_{核心功能}/计划.md`（覆盖 writing-plans 默认路径，产出后 `ls` 校验）。
  - **去 TDD**：明禁 writing-plans 的"写失败测试→跑红→实现→跑绿"五段式，改"动作→Expected"两段式，验证用嵌入式手段（编译/烧录/AT/抓包/长稳/功耗）；保留 no-placeholders。
  - **委托前 baseline 核查**：grep 目标文件，已就位的任务设计"核对/补位"双分支。
  - **Step 4 执行转交**：subagent-driven-development（推荐）/ executing-plans 二选一；执行后可选 spec-neoway-coding-standards 代码检查复核。
  - **硬依赖、无 fallback**：writing-plans 不在可用列表则直接终止。
  - **删除**被取代的 plan-template.md / anti-placeholder-examples.md / plan-reviewer-prompt.md（writing-plans 接管）。
  - **取舍**：v2.0 只产代码级计划，不再含项目级分期/验收矩阵/合入回滚/关键路径（按需另议）。
- **feat(skills)：实战检验微调（UIS8850 SETVOLTE 案例 b5b3c69d 回放）**
  - spec-requirement-generator 3.1→3.2：① 原始基准逐字保留（报文/日志/AT 响应禁美化）；② 提问维度加"初始状态"（默认值/掉电保存，必问）。
  - spec-solution-designer 1.4→1.5：③ 现状分析加"共享/变体文件适用范围核查"（命令表/配置/Kconfig/CMake/驱动适配）。
- **端到端验证**：用 b5b3c69d 案例回放测试 planner→writing-plans 管线，**设计验证有效**（产出代码级 计划.md，硬约束/去 TDD/代码精度/baseline 感知均达标）；但发现 **P0：技能未加载**——运行时用插件 cache（磁盘 1.3.1 / 会话注册表停 1.2.0），源 repo 的 v2.0 未生效。**修法：提交→push→`/plugin` 更新 spec-embedded-iot→重启会话**。
- **docs**：CLAUDE.md / README.md / spec-using-agents 的 planner 描述同步为"委托 writing-plans"；README 工作流与功能变更归档补 v1.4.0 演进注记。

## 2026-06-18（v1.3.1）

- **release：插件版本 1.3.0 → 1.3.1**（Neoway 编码规范贯穿方案/计划链路）。
- **feat(spec-solution-designer v1.4)：新增「宏控与生效边界」提问维度**——按编码规范 §3.4 给新增功能拟 2-3 个功能宏候选（`FEATURE_NWY_[AT|OPEN]_FUNC_[FUNC1]_[BZ]`）+ 推荐 + 生效边界（哪些项目/客户版本打开、回滚性）；模板 §9.2 加「生效范围」列。
- **feat(spec-solution-designer v1.4)：命名合规**——对外接口/函数/AT 命令/宏控命名遵循 spec-neoway-coding-standards（核心原则 + 模板 §9.1）。
- **feat(spec-implementation-planner v1.3)：编码规范全局约束**——所有任务代码须符合 spec-neoway-coding-standards，作为 `计划.md` §1.3「全局约束（所有任务隐含）」写入；WBS 代码任务验收点隐含规范检查；转交 superpowers 时明确要求生成代码合规。
- **目标**：让"所有代码符合编码规范"从 **方案→计划→实施** 全链路可执行——方案定合规命名、计划写全局约束、实施（人工/agent）都带得上。

## 2026-06-18（v1.3.0）

- **release：插件版本 1.2.0 → 1.3.0**（新增方案/计划两个技能，补全"需求→方案→计划"链路）。
- **feat(skills)：新增 spec-solution-designer 与 spec-implementation-planner**
  - 补全 spec-requirement-generator（需求）之后缺失的两环：**技术方案（方案.md）** 与 **实施计划（计划.md）**。
  - 三份文档并排在 `.spec/requirement/{项目ID}_{核心功能}/`（需求.md / 方案.md / 计划.md），路径与 generator 一致。
  - **设计决策**：拆两个独立技能（仿 superpowers 的 brainstorming/writing-plans 模块化）；与 superpowers 关系为"**借鉴非替代/包装**"——吸收其反空话、校准式自审、独立子会话复查等方法论，但 spec 保持自包含（嵌入式维度 + 项目级路径 + 模板），superpowers 仅作可选转交并优雅降级。
  - **spec-solution-designer（v1.3）**：备选方案选型（2-3 方案+权衡+推荐）→ 增量验证 → 方案自审（占位符/一致性/范围/歧义/YAGNI + 校准）→ 用户审批 gate；复杂方案可选独立子会话评审。
  - **spec-implementation-planner（v1.2）**：文件/模块结构映射 → 反空话 WBS + 接口契约（单一事实源）→ 依赖关键路径 → 验收验证矩阵；自审（覆盖/占位符/接口一致）；可选转交 superpowers:writing-plans / executing-plans，附**可观测判定**（仅当可用技能列表含该技能才提议）。
- **review：skill-creator 方法论审查两技能**（独立 reviewer 子会话 + 交叉核对），落地 11 项优化（H1 触发词按前置文件锚定 / H3 superpowers 去重+可观测降级 / M1 反空话 worked example / M2 防稻草人 / M3 gating 跳过 / M4 评审可观测阈值 / M5 跨文档引用按章节名 / L1–L5 描述精简·平台识别·分期后填等）；纠正审查误报 1 项（H2 KB 归档承诺）。详见 [功能变更/2026-06-18-方案与计划技能链路.md](./功能变更/2026-06-18-方案与计划技能链路.md)。
- **docs：新增 `docs/功能变更/` 子目录**归档本次功能改动；更新 docs/README 索引。
- **登记**：CLAUDE.md / README.md / spec-using-agents 技能表补两技能；spec-requirement-generator 下游指针接上 spec-solution-designer。

## 2026-06-17（v1.2.0）

- **release：插件版本 1.1.0 → 1.2.0**（知识库检索本轮增强：混合索引 + 审查优化 Tier 0–3 后的发版）。
- **chore(knowledge/ASR1603)：回填工作项ID + 纳入 rti_switchout bug 文档**——4 条历史文档补「工作项 ID = NA」（与 EC626 一致），新文档 `ASR1603_NA_rti_switchout_null_deref.md` 纳入；meta 标记 `schema_version=1`，重建 bug-solutions 向量索引，verify 通过。
- **feat(archiver/retrieval)：混合索引（摘要 + 正文分块）**
  - bug-solutions / requirement-solutions 新增 `index_body:true`：除摘要向量外，对其 `.md` 正文分块建 `body` 向量，补召回（日志/寄存器/AT 命令等正文细节可语义检索）。
  - 摘要向量 ID `{plat}/{file}`（向后兼容），正文块 ID `{plat}/{file}/chunk_{n}`。
  - 空摘要时经正文兜底仍可检索；既无摘要又无正文才警告跳过。
- **feat(search)：按源文件去重 + 命中类型标签**
  - 过取后按 `(platform,file)` 去重，避免 top-N 被同一文档多块占满。
  - 输出 `[摘要]`/`[正文]` 标签，`[正文]` 显示命中段落标题。
- **docs：新增 `docs/` 文档目录**（功能说明 / 历史修改 / 审查与优化）。
- **review：完成知识库检索架构多角度审查**，归档审查报告 + 优化方案（见 `审查与优化/2026-06-17-知识库检索架构审查报告.md`）。
- **fix(archiver/retrieval)：Tier 0 正确性修复（审查报告 C1–C4，全部已测）**
  - **C1 verify 向量校验复活**：移除不存在的 `COLLECTION_NAME` 导入，改用 `cfg["dest_dir"]` 定位 collection（顺带覆盖 requirement），`except:pass` 改为 warn。已证明检查4代码路径实际执行。
  - **C2 距离度量改 cosine**：所有 `get_or_create_collection` 加 `hnsw:space=cosine`，全量重建；`1-distance` 现为真实 cosine 相似度（相关查询 0.7–0.8，无关查询 ~0.24，区分度明显）。
  - **C3 chunk_markdown 去垃圾块**：大节按 `body` 切分（标题附加首段，不再独立成块）+ 空输入返回 `[]`；bug-solutions 索引 349→347（消除标题垃圾块）。
  - **C4 表格正则严格化**：`TABLE_ROW_RE` 用 `[^|]` 字符类，只匹配 2 列行，3+ 列表格不再被吞列污染（单测：2 列/加粗/list/分隔行均正确）。
- **fix(archiver/retrieval)：Tier 1 重要可靠性修复（审查报告 H1/H2/H3/H5/H6，全部已测）**
  - **H1 相似度阈值**：`embed_search` 加 `--min-similarity`（cosine，默认 0.0 不过滤；RAG 建议 0.3）。阈值 0.3 下相关查询保留、无关查询被过滤。
  - **H2 verify 反向僵尸校验**：检查4新增反向扫描，检出当前平台 meta 已不存在的 `[僵尸向量]`（端到端验证：注入假向量→检出→清理→恢复 OK）。
  - **H3 向量 id 与文件名解耦**：向量 id 基础从 `file` 改为 `entry_name`（`{plat}/{entry_name}[ /chunk_N]`），根治文件名迁移（加平台前缀）导致的 id 漂移；verify 检查4同步。已全量重建。
  - **H5 库函数抛异常**：`get_doc_type_config`/`get_collection_config` 改抛 `ValueError`（替代 `sys.exit`），便于作为库复用与测试；CLI 入口捕获转友好报错。
  - **H6 静默 except 降级**：摘要提取失败、`extract_summary --type` 加载失败等隐藏真实问题的 `except:pass` 改为 `[warn]` 到 stderr。
- **refactor(common)：Tier 2 架构/可维护性（审查报告 H4/H7/M5/M2/M3，全部已测）**
  - **H4 配置校验 + 深度合并**：`load_config` 深度合并默认值（缺键回退，防静默瘫痪）+ `_validate_config`（必需键、summary collection↔doc_type 关系、version 告警）；顺带消除 doc_type↔collection 隐式耦合（加载期断言）。
  - **H7 缓存安全**：`load_config` 返回 deepcopy 防污染；embedding 单例双检锁；新增 `reload_config()`。
  - **M5 统一正则**：抽 `SUMMARY_HEADING_RE` 常量；修复 ensure 的 `\s*$` 严格锚点导致带后缀标题（如「## 结构化摘要（专项）」）不注入 NA 的不一致。
  - **M2/M3 去重+过取**：去重 key 加 collection 维度；过取因子 `top*5`→`top*10`。
- **chore/test：Tier 3 工程化（依赖/分隔符/锁/测试，全部已测）**
  - 声明运行时依赖：`requirements.txt`（chromadb>=1.5、sentence-transformers>=5.0）。
  - doc_id 分隔符统一：`_collect_markdown_chunks` 的 rel_path/sub_path 统一正斜杠，跨平台一致（实测 code-summary 159 块无反斜杠）。
  - **H8 meta schema_version + 文件锁**：`save_meta` 标记 `schema_version=1`，`load_meta` 检查兼容性告警；归档临界区加跨平台独占文件锁（标准库 msvcrt/fcntl，零依赖），防并发 last-write-wins。
  - 补单元测试：`test_archiver.py`（unittest，24 项，覆盖 chunk_markdown/build_summary_text/表格解析/safe_filename/extract_*/ensure/deep_merge/validate）。

## 2026-06-16（v1.1.0）

- `34ff9c7` chore: 插件版本 1.0.0 → **1.1.0**
- `12bb92f` chore: 删除历史计划文档 multi-tool-plugin-adapter.md
- `36336e4` fix(docs): 修复 using-agents 改名后的引用一致性（session-start hook 路径）
- `6306760` chore(knowledge/ASR1603): 归档 PPPREDIAL与TCP自动重建 需求方案（端到端验证需求归档链路）
- `23d5c74` feat(spec-requirement): 对齐归档约定——路径/项目ID/结构化摘要/扁平子需求
- `02cf1ef` feat(spec-knowledge-archiver): **需求归档支持项目ID与主文档置顶**
  - requirement doc_type 增加 `项目ID`（summary_fields / index_columns），缺省 NA
  - `merge_md_files` 主文档置顶（含「结构化摘要」节优先），消除早期"主文档判定"问题
- `d25d556` feat(skills): **工作项ID 作为 bug 解决方案的必填追溯字段**
- `10fa9e9` chore: 重命名 using-agents 为 **spec-using-agents**，`.gitignore` 仅追踪 `spec-*` 技能
- `18c8a51` chore(knowledge/EC626): **回填工作项ID字段并恢复被误删的 bug 解决方案**（20 条）
- `2bd9057` fix(spec-knowledge-archiver): **修复归档误删旧文件并支持工作项ID缺省 NA**
  - 修复数据丢失 bug：迁移时改为"先写新文件再删旧文件"，`filename_changed` 强制重写
  - 新增 `ensure_summary_field_row`：摘要缺失 工作项ID 时注入 `| **工作项 ID** | NA |`

## 2026-06-15

- `d0cb926` refactor: **skills 从 submodule 转为 inline 目录**（此后按路径可追溯 git 历史）
- `f3ba135` chore: 更新技能表与 submodule 指针

## 2026-06-13

- `284e033` fix: 插件安装改用 URL source 格式以包含完整仓库
- `a09dd87` chore: 更新 skills submodule，修正 README dump analyzer 引用
- `1f53e3a` docs: 新增 README（安装、技能列表、工作流指南）
- `90e1f60` feat: 打包为 Claude Code 可分发插件
- `4472b69` chore(git): **gitignore vector_db**，更新 skills submodule
- `f94357c` feat(knowledge): 归档 EC626 平台官方文档
- `e56bee0` feat(knowledge): **新增多类型知识结构与配置**（doc_types + collections 双配置）

## 2026-06-12 及更早

- `7726ce8` chore: 更新 skills submodule（e0e63c2）
- 早期：确立按芯片平台组织的知识库结构、向量检索基础设施（ChromaDB + paraphrase-multilingual-MiniLM-L12-v2）。

---

## 待办（来自审查报告，未实施）

见 [审查与优化/2026-06-17-知识库检索架构审查报告.md](./审查与优化/2026-06-17-知识库检索架构审查报告.md)：

- **Tier 0（✅ 已于 2026-06-17 完成，见上方条目）**：C1 verify 复活 / C2 cosine / C3 去垃圾块 / C4 表格正则
- **Tier 1（✅ 已于 2026-06-17 完成，见上方条目）**：H1 阈值 / H2 反向校验 / H3 id 解耦 / H5 抛异常 / H6 静默 except
- **Tier 2（✅ 已于 2026-06-17 完成，见上方条目）**：H4 配置校验+深度合并 / H7 缓存安全 / M5 统一正则 / M2-M3 去重+过取
- **Tier 3（✅ 已于 2026-06-17 完成，见上方条目）**：声明依赖 / doc_id 分隔符统一 / H8 schema_version+文件锁 / 补单元测试

> ✅ **审查优化计划已全部落地**：Tier 0–3 共 17 项，均通过单元测试 / 端到端测试。

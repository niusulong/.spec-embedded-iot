# EC626 分析方法（仅差异）

> 平台：EC626（EigenComm ARM Cortex-M + FreeRTOS）。本文件只列与通用基线的差异，通用方法见 `SKILL.md` 与 `references/analysis-patterns.md`。
> 结构契约见 `_schema.md`。

## 根注解
ARM Cortex-M + FreeRTOS + LWIP（GCC arm-none-eabi / ARMCC）。下面的 memp / trace_node / FreeRTOS heap 差异源于此——**LWIP 内存池（memp）与 FreeRTOS 堆（pvPort）双轨制**是 EC 平台内存问题的核心。

## 代码地图
> 典型 SDK 布局：`PLAT/driver/middleware`（源自该项目 `项目概览.md` §2 目录结构映射，**以实际项目概览为准**——不同 EC626 项目布局可能不同）。
> 业务逻辑所在子仓/模块路径待实战补充（确认后再填，不臆测）。
> 锚点过期时（glob 扑空）按 `SKILL.md` Step 5 降级链回退，并提示更新本文件。

## 检索清单（仅平台独有，通用串不列）
`memp_malloc` | `LWIP OOM` | `freeHeap` | `pvPort_malloc` | `trace_node` | `ASSERT`

## 差异化定位手法（仅平台独有，通用手法不抄）
- **heap 失败点三数字判别**（malloc 崩溃 / 堆问题）：取三个数——失败 malloc 请求字节数 + 最大连续 free 块 + 总 free。
  - 请求 ≤ 最大连续 free 但 > 总 free → **碎片化**
  - 请求 > 总 free → **容量不足**
  - 先判类型，再定根因。

## 平台专属问题模式（仅本平台特有，通用 8 模式不重复）
| 现象 | 日志特征 | 根因方向 |
|------|----------|----------|
| memp 池耗尽 | `memp_malloc fail`、`LWIP OOM` | 某类 pbuf/socket 未释放，累积耗尽；查 `trace_node` 分配追踪 |
| FreeRTOS 堆耗尽/泄漏 | `freeHeap` 持续下降、`pvPort_malloc fail` | 大块未释放；查 `trace_node`、任务栈占用 |
| 栈溢出 | HardFault、`stack overflow`、栈水位检查告警 | 任务栈配置过小 / 递归或大局部变量 |
| ASSERT 触发 | 含 `文件名:行号` 的 ASSERT | 直接定位断言点，分析为何前置条件不满足 |
| 看门狗复位 | `WDT`、`IWDT`、长时间无喂狗 | 某任务死循环 / 被高优先级任务饿死 / 中断中阻塞 |

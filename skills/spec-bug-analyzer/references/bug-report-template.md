## 0. 结构化摘要

> 以下信息供知识库检索使用，需完整准确填写。

| 字段 | 内容 |
|------|------|
| **工作项 ID** | [缺陷单号，如 6977185133；必填，用于归档追溯] |
| **平台** | [精确填平台名，如 EC626 / ASR1603 / N58 / UIS8850 / UIS8852；**禁止带架构后缀/括号**，否则破坏归档后 --platform 向量检索] |
| **当前分支** | [分析时的 git 分支，如 master / feature/ipv6-fix；取自 `git rev-parse --abbrev-ref HEAD`，缺失填 unknown] |
| **模块** | [如 MQTT / LWIP / UART] |
| **问题分类** | [资源耗尽 / 状态机异常 / 参数错误 / 时序竞争 / 内存泄漏 / 协议异常 / 缓冲区溢出 / 超时] |
| **症状关键词** | [3-5 个关键词，逗号分隔，如: memp_malloc fail, 系统死机, 长时间挂测] |
| **根因概述** | [一句话描述根因，如: sock_event_queue 溢出导致 memp 未释放，累积耗尽触发 HardFault] |
| **调用链摘要** | [如: CoAP GET → sock_event_queue → memp_malloc → 耗尽 → HardFault] |
| **检索关键词** | [5-8 个检索词，中英文均可，逗号分隔] |

---

# [问题标题] 原因分析

## 目录
- [0. 结构化摘要](#0-结构化摘要)
- [1. 问题描述](#1-问题描述)
- [2. 根本原因](#2-根本原因)
- [3. 相关文件](#3-相关文件)

---

## 1. 问题描述

[简洁描述用户报告的故障现象]

**问题类型**：单日志分析 / 对比分析

## 2. 根本原因

[一句话总结根本原因，包含技术细节]

### 2.1 关键日志证据

> 根据实际提供的日志类型填写，未提供的类型删除对应小节。

#### AT命令日志

```
[AT日志关键片段]
```

#### 模块AP日志

```
[模块AP日志关键片段]
```

#### Dump日志（死机/崩溃问题时）

```
[Dump关键片段]
```

### 2.2 代码调用链

| 信息 | 值 |
|------|-----|
| **入口函数** | `func_A()` |
| **调用链** | `func_A() → func_B() → func_C()` |
| **问题位置** | `func_C()` 第 X 行 |

**调用链分析**：
- 问题发生在 `func_C()` 中的具体操作
- 调用链显示问题触发路径
- 结合代码分析根本原因

> **对比分析时包含**：
>
> #### 对比环境
>
> | 对比维度 | 正常日志 | 异常日志 |
> |----------|----------|----------|
> | 日志文件 | {正常文件} | {异常文件} |
> | 时间范围 | {范围} | {范围} |
> | 场景描述 | {场景} | {场景} |
>
> #### 关键差异点
>
> | 时间点 | 正常日志值 | 异常日志值 | 差异说明 |
> |--------|-----------|-----------|----------|
> | T1 | {值1} | {值2} | **差异** |
> | T2 | {值3} | {值4} | **差异** |

### 2.3 问题分析

[详细技术分析，包含：
- 数据对比（如：msglen=5646 > maxBufLen=4400）
- 流程说明（如：SSL握手过程中...）
- 时序分析（如：T1时刻发送请求，T2时刻超时）
- 错误码含义（如：-0x7200 = MBEDTLS_ERR_SSL_*）
]

### 2.4 问题复现路径

| 项目 | 内容 |
|------|------|
| **前置条件** | [系统/模块必须处于的初始状态，如：PPP链路已建立 + IPv6已分配] |
| **必要状态** | [触发前必须满足的条件，如：UDP socket未关闭 + 缓冲区未释放] |
| **操作步骤** | 1. [步骤1] 2. [步骤2] 3. [步骤3] |
| **复现概率** | [必现 / 约XX%，连续测试N次可复现] |
| **验证方法** | [如何确认问题已复现，如：出现特定错误码/日志/死机] |

> 概率性问题标注复现概率和触发频率；证据不足以推导完整路径时，输出已知条件和推测路径，标注"待验证"。

## 3. 相关文件

- [文件路径1]
- [文件路径2]

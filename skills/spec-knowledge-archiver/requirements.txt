# spec-knowledge-archiver 运行时依赖
# 用途：知识库归档（kb.py archive）+ LLM-Wiki 维护（kb.py wiki lint/status/guide）
#
# Python >= 3.9（开发/测试环境 3.12）
#
# 注：向量检索（chromadb + sentence-transformers）已废弃并移除
#     （2026-07 改走 LLM-Wiki 路线，检索靠 agent 读 wiki/INDEX.md 渐进加载）
# 注：LLM-Wiki 维护依赖当前会话的 agent（ZCode/Claude），脚本本身不调 LLM

# YAML frontmatter 解析（wiki 页必须有 frontmatter，lint/ensure_wiki_frontmatter 用）
PyYAML>=6.0

# 注：归档文件锁使用标准库（Windows msvcrt / Unix fcntl），无需第三方依赖

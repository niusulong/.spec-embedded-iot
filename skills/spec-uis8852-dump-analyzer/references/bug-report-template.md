# UIS8852 Dump 分析报告模板

> 复制本模板到 `.spec/bug/{工作项ID}_{问题描述}/Dump分析.md`。无工作项 ID 时按内容命名目录。`[]` 占位符按实际填写；证据不足的部分标"待验证"。

---

# UIS8852 / N706C AP 死机分析报告 — [一句话根因]

> **工作项 ID**：[ID 或"暂无，按内容命名"]
> **Dump 目录**：[dump 目录绝对路径]
> **分析日期**：[YYYY-MM-DD]
> **分析工具**：spec-uis8852-dump-analyzer（scripts/uis8852_analyze.py 等）

## 0. 结构化摘要

> 以下信息供知识库检索使用，需完整准确填写。字段定义见
> `spec-knowledge-archiver/references/section0-spec.md`（统一规范，禁止自由发挥）。

| 字段 | 内容 |
|------|------|
| **工作项 ID** | [缺陷单号；必填，用于归档追溯。无则填 NA] |
| **平台** | [纯平台名：UIS8852；**禁止带架构后缀/括号/版本号**] |
| **模块** | [如 MQTT / LWIP / UART / FOTA] |
| **bug_type** | [**只能从 10 个规范值选一个**：时序竞争 / 内存越界 / 状态机异常 / 参数校验缺失 / 资源泄漏 / 编码错误 / 配置错误 / 硬件相关 / 协议实现错误 / AT回码格式。映射规则见 section0-spec.md] |
| **症状关键词** | [3-5 个关键词，逗号分隔，如: ASSERT, mcause, ISR 崩溃] |
| **根因概述** | [一句话描述死机根因] |
| **调用链摘要** | [箭头串，如: osInterruptDispatch → ISR → 触发函数 → assert] |
| **检索关键词** | [5-8 个检索词，中英文均可，逗号分隔] |
| **兄弟文件** | [选填，同 bug 目录下其它文件，如 Bug分析.md / 修改方案.md] |

## 0.2 结论速览（TL;DR）

- **崩溃定性**：[ASSERT / EXCEPTION]，[assert 文件:行 / mcause 码]，PC=`0x...`（`函数名+偏移`）
- **崩溃中断**：IRQ `[N]` = `[中断源宏]`（[中断性质，如 "LTE 子帧定时中断"]），ISR = `[ISR函数]`
- **崩溃时刻函数**：`[触发崩溃的函数]`（[做了什么，如 "osMalloc(801)"]）
- **根因**：[两层：主因 + assert 直接触发]
- **修复优先级**：P0 [核心措施]

## 1. 崩溃现场

### 1.1 蓝屏 / 版本
| 项 | 值 |
|---|---|
| 固件版本 | `[gBuildRevision]` |
| 定制版本 | `[nwy_cust.h 版本串]` |
| gIsPanic | [1] |
| gBlueScreenAbortType | `[0xFE ASSERT / mcause code]` |
| g_osErrorLog | `[完整蓝屏字符串]` |

### 1.2 异常寄存器
| 字段 | 值 | 解读 |
|---|---|---|
| g_osAssert.core/line/pc | [...] | [...] |
| g_osException.mcause | [...] | [code=N，含义] |
| mepc | [...] | [函数] |
| g_osInterruptNest | [...] | [ISR / 任务上下文] |

### 1.3 反汇编确认
[贴 assert 点反汇编，`li a1,<line>` + `jalr osAssertHandler` 双重确认是哪条 assert]

## 2. 堆状态（若是 dlmalloc/assert 相关）

| 字段 | 值 |
|---|---|
| base / total | `0x... / 0x...`（...MB） |
| used / 使用率 | `...（...%）` |
| top_size | `0x...（...B）` |

## 3. 调用链（帧感知回溯 + 反汇编验证）

```
osInterruptDispatch(IRQ N)        ← 中断派发
 └─ [ISR](驱动/中断入口)
    └─ [中间函数]
       └─ [触发函数](做了什么)    ← ★
          └─ [assert/异常点]
```

[每步标注验证方式：反汇编 call site / g_osIrqNo / 源码调用图]

## 4. 深入分析（按需）

### 4.x [堆物理遍历 / malloc trace / ISR 日志源 / ...]
[贴脚本输出关键证据，给出判定]

## 5. 根因分析

1. **主因**：[...]，证据：[...]
2. **assert 直接触发**：[...]，证据：[...]

## 6. 修复建议
| 优先级 | 措施 | 针对 |
|---|---|---|
| P0 | [...] | [...] |
| P1 | [...] | [...] |

## 7. 复现路径
- **前置条件**：[...]
- **触发**：[...]
- **概率**：[必现 / 偶发 + 依赖条件]
- **待验证**：[证据不足的推测部分]

## 8. 附录
- 关键符号地址表
- 分析脚本（归档于 `scripts/`）
- dump 原始路径（大体积文件保留原位）

---

## 编写要点

1. **区分"已验证"与"推断"**：每个结论标注证据来源（脚本输出/反汇编/源码/推测）
2. **中断身份用 `g_osIrqNo` 权威确认**，不要只靠栈扫描猜
3. **崩溃时刻函数用帧感知回溯 + 反汇编 call site 验证**，不要轻信启发式扫描
4. **dlmalloc assert 先看堆使用率**：耗尽也会触发 539（dlmallocPrint 遍历撞异常）
5. **malloc trace ring 末尾非崩溃 alloc**：崩溃时刻调用者看栈，不看 ring
6. **复现路径必须给**：证据不足时给已知条件 + 推测，标"待验证"
